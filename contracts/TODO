☐ Clean up 1155D
    Note: Implemented a currentIndex to serve where MAX_SUPPLY was used for compilation.
☐ Consider adding a max population limit to the sashes to control the rate of growth as well as add speculative value to the subscriptions when they are enabled.
    Note: Closed betas are for chumps. Instead, just test in prod :)