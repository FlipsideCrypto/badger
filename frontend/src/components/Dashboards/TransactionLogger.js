import { Box } from '@mui/material';

const TransactionLogger = (props) => {
    const {logs, errors} = props;

    return(
        <Box sx={{
            mx: 'auto',
            width: '80%'
        }}
        >

        </Box>
    )
}