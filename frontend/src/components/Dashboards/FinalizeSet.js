import BigBox from "../Blocks/BigBox";


// TODO: This is the final dashboard where they will upload to ipfs, and run their transactions.
// TODO: Should be with breadcrumbs and a back and forth action button.

const FinalizeSet = (props) => {
    const { 
        setStage, 
        badgeSetImageFile, 
        badgeSetDeploymentArgs, 
        badgeDataToFinalize, 
        address, 
        signer
    } = props;

    return (
        <>
        </>
    )
}

export default FinalizeSet;